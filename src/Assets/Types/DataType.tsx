export type Data = {
  drinks: [{ strDrink: string; strDrinkThumb: string; idDrink: string }];
};

export type CategoryData = {
  drinks: [{ strCategory: string }];
};

export type GlassesData = {
  drinks: [{ strGlass: string }];
};

export type AlcoholicData = {
  drinks: [{ strAlcoholic: string }];
};
